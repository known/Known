@model Known.WebMvc.Models.SplitPageViewModel
<div class="mini-splitter" style="width:100%;height:100%;">
    <div id="splitLeft" tooltip="关闭/显示菜单" size="150px" showCollapseButton="true" style="padding:5px;">
        @if (!string.IsNullOrWhiteSpace(Model.LeftPartialName))
        {
            @Html.Partial(Model.LeftPartialName)
        }
    </div>
    <div id="splitRight">
        @if (!string.IsNullOrWhiteSpace(Model.RightPartialName))
        {
            @Html.Partial(Model.RightPartialName)
        }
    </div>
</div>
@section Foot {
    @Html.MinScript("static/modules/frame/" + Model.JsPath + ".min.js")
}