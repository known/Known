@inherits BaseComponent
@attribute [NavItem]

<Tooltip Title="@Language["Nav.Setting"]">
    <Icon Type="setting" OnClick="@(e=>Topbar?.OnMenuClick?.Invoke("setting"))" />
</Tooltip>

@code {
    [CascadingParameter] private TopNavbar Topbar { get; set; }
}