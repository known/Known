@inherits BaseComponent

@if (!string.IsNullOrWhiteSpace(Icon) && Visible)
{
    if (!string.IsNullOrWhiteSpace(Title))
    {
        <Tooltip TitleTemplate="TitleTemplate(Title)">
            @RenderItem()
        </Tooltip>
    }
    else
    {
        @RenderItem()
    }
}