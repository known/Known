@inherits BaseComponent

@if (Visible)
{
    <div class="kui-title">
        @if (ChildContent != null)
        {
            @ChildContent
        }
        else
        {
            if (!string.IsNullOrWhiteSpace(Text))
            {
                <span>@Language[Text]</span>
            }
            if (!string.IsNullOrWhiteSpace(SubText))
            {
                <span class="kui-sub-title">@Language[SubText]</span>
            }
        }
    </div>
}