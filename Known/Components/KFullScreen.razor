@inherits BaseComponent

<div class="@Class">
    @if (!isFullScreen)
    {
        <div class="action">
            <Tooltip Title="@Title">
                <Icon Type="@Icon" OnClick="OnFullScreenAsync" />
            </Tooltip>
        </div>
    }
    @ChildContent
</div>